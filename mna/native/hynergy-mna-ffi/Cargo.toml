[package]
name = "hynergy-mna-ffi"
version.workspace = true
edition.workspace = true

[lib]
name = "hynergy_mna"
crate-type = ["cdylib"]

[dependencies]
network-engine = { path = "../network-engine" }
network-protocol = { path = "../network-protocol" }
